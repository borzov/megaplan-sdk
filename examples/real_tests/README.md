# Integration Tests for Megaplan SDK

Интеграционные тесты для проверки работы SDK с реальным API Megaplan.

## Структура тестов

- **test_employees.py** - тесты для работы с сотрудниками (Employees)
- **test_tasks.py** - тесты для работы с задачами (Tasks)
- **test_deals.py** - тесты для работы со сделками (Deals)
- **test_projects.py** - тесты для работы с проектами (Projects)
- **test_contractors.py** - тесты для работы с контрагентами (Contractors)
- **run_all.py** - запуск всех тестов одной командой
- **utils.py** - вспомогательные функции

## Настройка

### 1. Создайте файл `.env`

Скопируйте файл `.env.template` в `.env`:

```bash
cp .env.template .env
```

### 2. Заполните учетные данные

Отредактируйте файл `.env`:

```bash
MEGAPLAN_BASE_URL=https://yourcompany.megaplan.ru
MEGAPLAN_USERNAME=your@email.com
MEGAPLAN_PASSWORD=your_password
```

**ВАЖНО:**
- Файл `.env` не должен попадать в git (уже добавлен в .gitignore)
- Если в пароле есть спецсимволы, они будут корректно обработаны

### 3. Убедитесь, что SDK установлен

```bash
cd /Users/borzov/Develop/Public/megaplan-sdk
pip install -e .
```

## Запуск тестов

### Запуск всех тестов

```bash
cd examples/real_tests
python3 run_all.py
```

Это запустит тесты для всех ресурсов (employees, tasks, deals, projects, contractors).

### Запуск отдельных тестов

Вы можете запустить тесты для конкретного ресурса:

```bash
# Тесты сотрудников
python3 test_employees.py

# Тесты задач
python3 test_tasks.py

# Тесты сделок
python3 test_deals.py

# Тесты проектов
python3 test_projects.py

# Тесты контрагентов
python3 test_contractors.py
```

## Что тестируется

### Employees (Сотрудники)
1. ✅ Получение текущего пользователя (`get_current`)
2. ✅ Получение списка сотрудников (`list`)
3. ✅ Получение сотрудника по ID (`get`)
4. ✅ Поиск сотрудников (`list` с параметром `q`)

### Tasks (Задачи)
1. ✅ Получение списка задач (`list`)
2. ✅ Получение задачи по ID (`get`)
3. ✅ Получение комментариев к задаче (`get_comments`)
4. ✅ Получение полной информации о задаче (`get_full_details`)

### Deals (Сделки)
1. ✅ Получение списка сделок (`list`)
2. ✅ Получение сделки по ID (`get`)
3. ✅ Получение комментариев к сделке (`get_comments`)
4. ✅ Получение полной информации о сделке (`get_full_details`)

### Projects (Проекты)
1. ✅ Получение списка проектов (`list`)
2. ✅ Получение проекта по ID (`get`)
3. ✅ Получение задач проекта (`get_tasks`)
4. ✅ Получение полной информации о проекте (`get_full_details`)

### Contractors (Контрагенты)
1. ✅ Получение списка контрагентов (`list`)
2. ✅ Получение контрагента по ID (`get`)
3. ✅ Поиск контрагентов (`list` с параметром `q`)
4. ✅ Получение сделок контрагента (`get_deals`)

## Примеры вывода

### Успешный тест
```
======================================================================
  ТЕСТИРОВАНИЕ: СОТРУДНИКИ (EMPLOYEES)
======================================================================

👤 Текущий пользователь:
   ID: 1000003
   Имя: Максим Борзов
   Email: m.borzov@example.com
   Должность: Генеральный директор

✅ Текущий пользователь загружен

======================================================================
  ИТОГОВАЯ СТАТИСТИКА
======================================================================

📊 Результаты тестирования:
   Всего тестов: 4
   Успешно: 4
   Провалено: 0

✅ ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО! 🎉
```

## Устранение неполадок

### Ошибка 401 (Unauthorized)
Проверьте правильность учетных данных в `.env` файле.

### Ошибка 404 (Not Found)
Убедитесь, что в вашем аккаунте Megaplan есть данные для тестирования (задачи, проекты, сделки и т.д.).

### Тайм-аут соединения
Проверьте доступность вашего Megaplan инстанса по URL из `MEGAPLAN_BASE_URL`.

### Тесты падают с ValidationError
Это может означать несоответствие модели SDK и реальных данных API. Откройте issue в репозитории с подробным описанием ошибки.

## Логирование

По умолчанию тесты используют уровень логирования `INFO`. Вы можете изменить уровень в коде тестов:

```python
setup_logging("DEBUG")  # Подробное логирование
setup_logging("WARNING")  # Только предупреждения и ошибки
```

## Безопасность

**⚠️ ВАЖНО:**
- Никогда не коммитьте файл `.env` в git
- Не используйте production аккаунт для тестов
- Тесты выполняют только операции чтения (GET запросы)
- Никакие данные не изменяются и не удаляются

## Разработка новых тестов

Чтобы добавить новый тест:

1. Создайте функцию в соответствующем файле `test_*.py`
2. Добавьте вызов функции в `run_all_tests()`
3. Используйте утилиты из `utils.py` для единообразного вывода

Пример:

```python
async def test_my_feature():
    """Test description."""
    print_header("TEST: My Feature")

    load_env_file()
    credentials = get_credentials()
    if not credentials:
        return False

    base_url, username, password = credentials

    try:
        async with MegaplanClient(
            base_url=base_url,
            username=username,
            password=password
        ) as client:
            # Your test code here
            result = await client.resource.method()
            print_success("Test passed")
            return True
    except Exception as e:
        print(f"\n❌ Error: {e}")
        return False
```

## Лицензия

Тесты распространяются под той же лицензией, что и Megaplan SDK.
